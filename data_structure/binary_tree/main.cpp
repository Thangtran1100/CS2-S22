#include "tree.h"

int main()
{
    Node *root = NULL, *node;

    node->insert(root, 1);
    node->insert(root, 2);
    node->insert(root, 3);
    node->insert(root, 4);
    node->insert(root, 5);

    node->printPreOrder(root);

    return 0;
}